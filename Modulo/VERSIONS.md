# Modulo Version History

| Version | Git Tag | What changed |
|---------|---------|-------------|
| v1 | `modulo-v1` | HTML prototypes, Figma extraction |
| v2 | `v2-modulo` | React app, design system page, all core screens |
| v3 | current (`main`) | "Put it all to work" optimisation, autopilot, achievements, settings, send amount, iPhone 17 Pro frame, DS page redesign, agentic skills |

To view a previous version:
```bash
git stash && git checkout v2-modulo    # view v2
git checkout main && git stash pop     # back to current
```
