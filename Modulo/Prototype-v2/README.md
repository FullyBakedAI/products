# Modulo Prototype v2 — Cold Start Test

This directory is intentionally empty. It tests whether the build pipeline
can produce accurate UI from nothing but the Figma file and product.json.

**Build instructions:** See `../CLAUDE.md`
**Product config:** See `../product.json`

No pre-existing tokens, assets, or shared CSS. Everything generated fresh.
