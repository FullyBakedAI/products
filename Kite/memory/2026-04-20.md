# Kite — 2026-04-20

## What happened

- Discovery phase completed (07:00–09:00 GMT+1): family travel research with Ant and Lorena. See `travel-planner-next/DISCOVERY.md`.
- Design phase completed (09:00–09:20 GMT+1): four component specs shipped with ASCII mockups. See `travel-planner-next/DESIGN.md`.
- Next.js app built: Plan + Journal modes, Firebase sync, Copilot, map panel, Inspirations Board component, memory timeline, highlight reel.
- 10 commits pushed to `github.com/FullyBakedAI/travel-planner-next` through the day.
- Vercel project `travel-planner-next` linked, auto-deploy live at `travel-planner-next.vercel.app`.

## What Ant flagged (09:20 GMT+1)

The spawn that built this app did NOT follow the standard agent pattern: no context wiring, no memory saving, no daily logs, and the app sat untracked in the workspace git. Directive: "make sure this never happens again."

Response: added the **Spawn Pre-Flight Checklist** to `subagent-policy.md` + CLAUDE.md, saved `feedback_spawn_preflight.md` memory, and created this Product hub (`~/.openclaw/Products/Kite/`) with proper CONTEXT/STATUS/memory wiring. Gitignored `travel-planner-next/` from workspace repo since it has its own remote.

## What worked

- Discovery → Design → Build in a single day is viable when specs are grounded in explicit principles (10 design + 11 product).
- ASCII mockups are 3–4× faster than Figma for design spec turnaround, and Ant prefers the speed.
- DISCOVERY.md + DESIGN.md + CONTEXT.md inside the app repo kept context portable and close to the code.

## What didn't work

- Spawn was launched without Product hub wiring. Build happened in isolation — no daily logs, no memory handoff, no `~/.openclaw/Products/Kite/` directory. Fixed today.
- Lorena's simple Stop Card view still missing from the build despite being explicit in DESIGN.md.
- Claude API trip drafting (Step 2 of creation flow) not wired — blocks the full flow.

## What's next

See `../STATUS.md` "Next steps" section. Immediate priorities: Lorena view, Claude API wiring, Inspirations Board integration, Album printable output — in that order.

---

## Evening session (2026-04-20 20:30–20:45 GMT+1, #kite)

Ant pushed back on the design system being "up in the air". Conversation pivoted from incremental work to a full rebuild decision.

**Three decisions made:**

1. **Rebuild over retrofit (Option B)** — `decisions/2026-04-20-rebuild-decision.md`
2. **Drop Firebase BYO from Kite** — `decisions/2026-04-20-drop-firebase.md`
3. **Canonical BakeKit = Modulo BakeKit** — `decisions/2026-04-20-canonical-bakekit.md`

**v1 ship list (narrow):** Multi-trip home, Stop Card (Lorena+Ant), Plan creation, Journal.
**Deferred to v2:** Inspirations Board, Album printable, Map, Highlight Reel, Memory Timeline, Settings page.

**Three pending decisions blocking scaffold** (asked Ant, awaiting answer):
1. Repo: new `kite` vs reset `travel-planner-next` main
2. Backend timing: localStorage-only for SF + Supabase ADR in parallel
3. Code location: `~/.openclaw/workspace/kite/`

**Workspace state captured into the canonical hub** (per Ant: "save this latest version to Kite as the kite files - change all the links to reflect this change"). Updated: STATUS.md, CLAUDE.md, AGENTS.md, MEMORY.md (this hub) + workspace cross-refs + memory entries pending in same session.
